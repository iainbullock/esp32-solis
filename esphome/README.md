# ESPHome configuration

This directory will contain sanitised ESPHome configuration examples for connecting the component to a Solis-based installation.

Personal credentials, device identifiers, network details, and site-specific settings must remain outside the repository. Examples should refer to values stored in ESPHome's `secrets.yaml` file where appropriate.
